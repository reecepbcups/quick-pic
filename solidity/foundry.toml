[profile.default]
src = "src"
out = "out"
libs = ["node_modules"]

# Remappings for npm-based dependencies
remappings = [
    "forge-std/=node_modules/forge-std/src/"
]

# Optimizer settings
optimizer = true
optimizer_runs = 200

# Solidity version
solc_version = "0.8.20"
